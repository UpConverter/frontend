import { Box } from '@mui/material';
import type { FC } from 'react';

import styles from './MainPage.module.css';

const MainPage: FC = () => {
    return <Box className={styles.mainPage}>Main page tst</Box>;
};
export default MainPage;
